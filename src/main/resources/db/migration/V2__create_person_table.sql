CREATE TABLE person (
    id_person int GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    ito_person_code VARCHAR(36) UNIQUE NOT NULL,
    name VARCHAR(120) NOT NULL,
    device_code VARCHAR(36) NOT NULL
);